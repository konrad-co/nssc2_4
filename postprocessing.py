from elementtest import *
import matplotlib.pyplot as plt

#useful data
coordinates=nodeToCoordinate((np.array(range(N*N)).reshape((N,N))),N,L)



plt.figure()
#plt.contour(coordinates,sol.reshape((N,N)))
